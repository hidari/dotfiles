---
status: open
---

# docs: 両リポジトリの Issue をマイルストーンへ整理し着手順を決める

## 背景

dotfiles と agentic-coding-tools に open Issue が積み上がっている (2026-08-25 時点で
両リポ合わせて 46 件)。それぞれの本文には順序制約が書かれているが、散文の中にしかないため
全体を俯瞰する手段が無い。着手のたびに全件を読み直すことになり、読み直さないまま着手すると
先行条件を踏み外す。

この Issue はその俯瞰を 1 箇所に置き、着手順の根拠を記録する。個々の作業は各マイルストーンが
指す Issue が持つ。

### 整理が必要だと分かった経緯

in-repo Issue の検査が配布先で走っていない、という問題を調べる過程で、同じ問題に触れる Issue が
両リポに散在していることが分かった。触れている Issue は上流だけで 9 件あるが、それを担当する
Issue は 1 件も無い。

同時に、判断材料そのものが腐っていることも分かった。参照先が closed になったことを参照元が
知らない stale な参照が両リポにある。訂正はタスクが持つ。

## 実測 (2026-08-25)

着手順の根拠になるものだけを挙げる。件数は日付付きのスナップショットとして扱うこと。

### 記法移行の母数

上流 ISSUE-22 の方法 (`issue.md` を持つディレクトリを `closed/` 込みで数え、`.cache` 配下の
probe を除く) で数え直した結果、旧記法 930 件 / 新記法 32 件 / 旧記法を持つリポジトリ 12。

同じセッションで最初に出した値は 833 件 / 11 リポジトリだった。ずれた要因は 2 つある。

1. 1 段深い場所にあるリポジトリ 1 件を漏らした。`find -maxdepth 3` では届かない
2. 母集団が違った。ディレクトリ数を数えており、`issue.md` を持つものに絞っていなかった

ISSUE-22 は 2026-08-24 時点で旧記法 911 件と記録しており、この 930 件との差は 1 日ぶんの
起票と読める。ISSUE-22 自身も計上を 3 回訂正しており、外れる方向は過小と過大の両方だった。

要因 1 は並行して調べていた別セッションも踏んでいる。ISSUE-22 の訂正記録にも同型の漏れが
あるが、そちらが挙げる件数と今回漏らしたリポジトリの件数は一致しないため、同一のリポジトリを
指しているかは確かめていない。

### ディレクトリ名を番号だけにする案は採れない

Issue 43 の案 2 を対照付きで測った。

| ディレクトリ名 | rc | `--next` の出力 |
| --- | --- | --- |
| `43_sometitle` (対照・旧記法) | 0 | ISSUE-44 |
| `ISSUE-43_sometitle` (対照・新記法) | 0 | ISSUE-44 |
| `43` (番号のみ) | 0 | ISSUE-1 |
| `ISSUE-43_` | 0 | ISSUE-44 |

番号のみは採番スクリプトの走査パターンに一致せず、既存の 43 が見えないまま rc=0 で
ISSUE-1 が返る。ISSUE-22 は「rename は採番を止めうる」を理由に rename を退けたが、実際には
止まるより悪い形 (沈黙する番号再利用) を踏む。この案は採らない。

### Issue クローズの書き換えコストの原因は移動

Issue 43 が挙げる書き換えコストの原因を、直近のクローズ (`fb72a36`) で確かめた。原因は
`closed/` への移動でパスの深さが 1 段変わることで、`--name-status` と `-U0` で見たところ
外部ファイル側の変更は例外なく相対リンクの補正だった。件数は Issue 43 が持つ。

壊れる対象がリンクだけなので、リンクを張らなければ移動を残したまま書き換えが 0 になる。
一覧性も失わない。

### 検査を取り付ける土台が大半のリポジトリに無い

`.pre-commit-config.yaml` を持つのは 3 リポジトリだけで、残りは pre-commit 自体が未導入。
検査の取り付けを論じる前に、取り付け先が存在しない。母集団は上の記法移行と同じ走査。

加えて apm がデプロイしたものは追跡下に無いため、git hook からも CI runner からも存在が
保証されない。配布側から `apm install` 時の自動セットアップを仕掛けることもできない
(`apm lifecycle trust` の対象は消費側の宣言で、これは配布側スクリプトの自動実行を防ぐ
意図的な設計)。

## マイルストーン

各マイルストーンの詳細は、それが指す Issue が持つ。この表が持つのは所属だけで、順序制約は
下の順序制約表が canonical である。両方に書くと同じ制約が 2 箇所へ散る。

所属の canonical もこの表で、各 Issue の側には書き戻さない。順序制約だけは先行される側の
本文にも書く (着手時に読むのはそちら側なので、片側だけだと届かない)。

| # | マイルストーン | 指す Issue |
| --- | --- | --- |
| M1 | Issue 維持コストの原因を消す | Issue 43、ISSUE-52 |
| M2 | 検査を配布先で走る状態にする | ISSUE-64、ISSUE-53、ISSUE-72 |
| M3 | skill バンドルの二重登録を止める | Issue 40 |
| M4 | PUBLIC 露出の scrub | Issue 21、Issue 29、上流 ISSUE-15、上流 ISSUE-23 |
| M5 | 静的検査の基盤を寄せ射程の穴を塞ぐ | Issue 39、Issue 30、ISSUE-57、ISSUE-60、Issue 41、ISSUE-47、Issue 17、Issue 18 |
| M6 | winvm の実装品質 | 上流の winvm 系 Issue |
| M7 | 環境とテスト網の個別 | 下の細分表 |
| M8 | CI の必須チェックを実効化する | ISSUE-50 |
| M9 | フックの共通基盤とガードの穴 | Issue 26、ISSUE-58、ISSUE-67、ISSUE-71、ISSUE-62、ISSUE-63、ISSUE-66 |
| M10 | 規範層の構造を直す | ISSUE-48、ISSUE-61、ISSUE-65 |

### M7 の細分 (2026-09-02)

起票時の M7 は「dotfiles の zsh / bats / PowerShell / Raycast クラスタ」という散文で、識別子を
持っていなかった。俯瞰から引けないので細分する。16 件と最大のマイルストーンだが、群のあいだに
依存が無いので着手順は自由に選べる。群の内側の順序制約も順序制約表が持つ。

| 群 | 指す Issue |
| --- | --- |
| zsh 層 | Issue 11、Issue 12、Issue 34、Issue 44、ISSUE-69 |
| テスト網 | Issue 4、Issue 45 |
| CI の安定性 | Issue 37、Issue 38 |
| Raycast | Issue 22、Issue 23 |
| PowerShell | Issue 27、Issue 28 |
| 単独 | Issue 31、Issue 33、ISSUE-51 |

### 割り当ての検算 (2026-09-02)

active 44 件の全件がいずれかのマイルストーンへ入ることを数えて確かめた。M1 が 2、M2 が 3、
M3 が 1、M4 が 2 (dotfiles 側のみ)、M5 が 8、M7 が 16、M8 が 1、M9 が 7、M10 が 3、この Issue
自身が 1 で合計 44。

最初に数えたときは 43 で、ISSUE-71 が抜けていた。**所属を決めただけでは取りこぼしは見えない。
全件が入ることを数えて初めて分かる。**

### マイルストーンの構成の変遷

M9 は 2026-08-28 に置いた。ISSUE-54 / ISSUE-55 / ISSUE-56 の 3 件を割り込みで実装して closed に
したあと、そこから派生した Issue の行き先が無かったためである。当時の本体 ISSUE-59 は closed に
なり、本体は Issue 26 (フックの共通基盤) へ移った。ISSUE-58 / ISSUE-67 / ISSUE-71 / ISSUE-66 の
4 件はいずれも `home/.claude/hooks/` のフックを対象にしており、Issue 26 の集約範囲と重なる。

M9 の射程を「PreToolUse とコミット時のガード」から「フックの共通基盤とガード」へ広げたのは、
ISSUE-58 (SessionStart の注入) と ISSUE-67 (PostToolUse の閾値通知) が PreToolUse ではないため。
起票時の名前のままだと、この 2 件がどこにも属さない。

ISSUE-60 と ISSUE-61 は M9 から動かした。ISSUE-60 は静的検査の対象選択なので M5、ISSUE-61 は
規約そのものの構造なので M10 にあたる。どちらもガードの穴ではなく、検査の射程と規範の形の話で
ある。

M1 の「指す Issue」に ISSUE-52 を足したのは、扱う対象が同じだから。M1 は Issue 本文から
リンクを外す方向なので、PR 本文の Closes 行がリンクの残る最後の場所になる。上流 ISSUE-24 と
同じテーブルで裁定するなら ISSUE-52 もそこへ持ち込む。

### 俯瞰の訂正 (2026-09-02)

着手の根拠にする前に dotfiles の active 44 件を全件読み直した。表は 4 箇所で実態と違っていた。

1. **M3 の上流側 ISSUE-27 は closed になっていた。**表は「起票済み・実装前で中断」と書いて
   いたが、2026-08-26 の PR #24 でマージ済みで、Issue 40 の本文にもその記録がある。M3 に残る
   のは dotfiles 側の 4 タスクだけ
2. **M9 の本体 ISSUE-59 は closed になっていた。**
3. **M1 の「実質完了」は言いすぎだった。**Issue 43 の残タスクが漸進的なのは事実だが、
   ISSUE-52 は 0/6 で未着手のまま残っている
4. **M2 の先頭は ISSUE-53 ではなく ISSUE-64 だった。**ISSUE-53 の本文が「ISSUE-64 が土台を
   作る側。着手はあちらが先」と明記している。表は ISSUE-64 を一度も載せていない

08-28 以降に起票された 11 件が、どれもマイルストーンへ所属していなかった。**割り込みで起票した
Issue が俯瞰から漏れる形は 08-28 の訂正で既に 1 度踏んでいる。**同じ形が 5 日で再発した。
所属を決める手順が起票の側に無いことが原因なので、手順へ足すかどうかを下のタスクに挙げる。

### 順序制約 (2026-09-02 に本文から抽出)

起票時の表の「先行」列は M1 から M2 への 1 件しか持っていなかった。実際には順序制約は各 Issue の
本文に書かれており、俯瞰へ上がっていなかっただけである。全件を読んで抽出した。この表が順序制約の
canonical で、マイルストーン表と細分表は所属だけを持つ。

| 先行 | 後続 | 根拠 |
| --- | --- | --- |
| ISSUE-64 | ISSUE-53 | pre-commit を持たないリポには検査を載せる土台が無い |
| Issue 43 | ISSUE-53 | baseline がファイルパスで違反を記録するので、`closed/` への移動の扱いが先に決まる必要がある |
| Issue 39 | Issue 30 | フェンス走査の 3 つ目を別実装で足さない |
| Issue 37 | ISSUE-50 | 必須チェックを入れると取得の一時障害がマージを止める |
| ISSUE-58 | Issue 26 の必須フック検査タスク | handoff-sentinel の位置づけを先に決める |
| Issue 44 | ISSUE-69 (逆でもよい) | `.zshrc` の置き場が衝突する |
| Issue 4 | Issue 45 (逆でもよい) | 同じファイル群を触る |
| ISSUE-72 | ISSUE-53 (逆でもよい) | 全リポの母集団を共有し、先に着手した側が形を決める |
| Issue 17 | Issue 18 (同時が自然) | 同種の検査で、片方を入れるときもう片方も入れる |
| Issue 22 | Issue 23 | 同一スクリプトの別の欠陥 |
| 上流 ISSUE-27 | Issue 40 | bare 名の解決が flat 配置に依存する (上流は closed で書き足せない) |

抽出の時点で先行される側の本文に制約が書かれていなかったのは 4 組ある。Issue 37 / Issue 4 /
Issue 17 は片側にだけ書かれており、Issue 22 と Issue 23 はどちらにも無かった。いずれもこの
Issue の作業で欠けている側へ書いた。

**「どちら側に書かれているか」は表の列として持たない。**当初は列にしたが、同じ変更で欠けている
側へ書き足すので、その列はコミットされた瞬間に偽になる。他 6 ファイルの本文の有無を写した値でも
あるので、以後それらを編集するたび黙って腐る。

抽出して分かったのは、**制約が「衝突する」という形で書かれているものが多い**ことである。
どちらが先でもよいが同時には触れない、という関係は先行関係の表では表しにくい。上の表では
「逆でもよい」と注記して残した。

**この表が網羅であることを見る機構は無い。**Issue 43 から ISSUE-53 への制約は本文の別の節
(「M1 が M2 に先行する理由」) が持っており、最初の抽出では漏れていた。レビューの指摘で足している。
同じ形の漏れは、本文で先行と書かれた組がこの表にあるかを検査する機構が無い限り再発する。

### 俯瞰の訂正 (2026-08-28)

この Issue 自身が腐っていた。以下は実測で確かめた訂正である。

**M1 は実質完了している。** (この判断は 2026-09-02 に訂正した。上の「俯瞰の訂正 (2026-09-02)」
の 3 を参照。ISSUE-52 が 0/6 で未着手のまま残っているので「実質完了」は言いすぎだった)
Issue 43 は 8 タスク中 7 が `[x]` で、残る 1 つは「baseline を減らす。Issue を触ったついでに
1 件ずつ」という漸進タスクである。着手すべき離散作業として残っているのは ISSUE-52 だけになった。

**下のタスク欄の「M1 を上流へ起票し、ISSUE-24 と同じテーブルで裁定する」は既に済んでいる。**
上流 ISSUE-24 は closed で、裁定 (2026-08-26) と適用 (2026-08-26) の節を持つ。Issue 43 側も
`[x] 決定を上流 ISSUE-24 へ持ち込む` と記録しており、裁定の結果「新規 Issue は立てず追記」と
決まったので起票は行われなかった。この Issue のタスク文だけが起票を前提にしたまま残っていた。

**割り込みが 3 件あり、どれもこの表に載っていない。** ISSUE-54 (gitleaks の検出網) /
ISSUE-55 (apm ガードの判定点) / ISSUE-56 (tirith の fail-open) を 2026-08-26 から 08-28 に
かけて実装しクローズした。いずれもセキュリティ層で、CLAUDE.md の「セキュリティを第一に
考える」に基づく意図的な割り込みだったが、マイルストーンへ所属させていなかったため俯瞰から
は見えなくなっていた。この 3 件から派生した ISSUE-59 から ISSUE-63 も同じ状態にある。

割り込み分を M9 として置く。既存の M1 から M8 の順序制約には影響しない。

### 参照の解決規則が 2 つある (2026-08-28)

`## 関連` 節の識別子を、検査と検索手順が別の規則で解決している。

- `config_guard.related_refs` は番号だけで解決する (`^(?:ISSUE-)?([0-9]+)_`)。旧記法と
  新記法を同一視するので、`ISSUE-26` と書いても `26_...` へ解決して緑になる
- `dev-workflow:in-repo-issue` の検索手順は識別子の文字列全体で引く
  (`-path "*/${ID}_*/issue.md"`)。`ID=ISSUE-26` では 0 件になる

したがって「検査は通るのに、手順どおりに探すと辿れない参照」が成立する。dotfiles 側で
10 件あり、この Issue の作業で旧記法へ揃えた。上流側は数えていない。

数えるときは他リポジトリ参照を除くこと。前置を無視して数えると
`agentic-coding-tools の ISSUE-22` のような正しい参照まで拾い、この Issue の作業では
実際に 23 件と過大に出した (正しくは 10 件)。

### 着手順の裁定 (2026-09-02)

**M2 の ISSUE-64 を先頭に置く。M1 の優先は解除する。**

理由は 2 つある。

1. ISSUE-64 はセキュリティ層にあたる。PRIVATE の 2 リポジトリに秘密の混入を止める層が無い。
   非公開であることは 1 層目で、gitleaks は 2 層目である。user CLAUDE.md の「防御を 1 層に
   頼らない」に直接かかる
2. M1 に dotfiles 内で完結する離散作業がほとんど残っていない。Issue 43 の残タスクは「触った
   ついでに 1 件ずつ」の漸進で、ISSUE-52 は上流への委譲である

**M1 が M2 に先行するという制約は、ISSUE-64 には掛からない。**制約の根拠は下の「M1 が M2 に
先行する理由」節が持つ。ISSUE-64 は baseline を作る作業ではなく pre-commit と gitleaks の
取り付けなので、その根拠が要求するパスの扱いに触れない。

08-27 の裁定は M2 の中身が ISSUE-53 だけだった時点のものである。ISSUE-64 が加わったことで
先頭が制約の外へ出た。**マイルストーン単位で引いた先行関係は、中身が増えると成り立たなく
なることがある。**制約は Issue 単位で持たせる方が壊れにくい。

M8 (ISSUE-50) を後回しにする 08-27 の判断は維持する。加えて Issue 37 が M8 の先行にあたる
ことが分かったので、M8 へ着手するなら Issue 37 が先になる。

### 着手順の裁定 (2026-08-27)

**M1 を優先する。M8 (ISSUE-50) は後回し。**

M1 を先に置くのは、M2 の baseline がパスに依存するという既存の理由に加えて、ISSUE-52 で
同じ対象の欠陥が実際に稼働中のプロジェクトへ出たため。上流 ISSUE-24 が未決のまま残っている
ぶんだけ、判断待ちが下流に溜まる。

M8 を後回しにするのは、CI が緑であることの確認が手作業 (SHA を突き合わせてジョブ件数を数える)
で今のところ回っているため。取りこぼしのリスクは残るが、判断待ちを増やしてはいない。

### 件数の推移

日付付きスナップショットとして扱うこと。着手順を決める根拠に使うときは必ず数え直す。

| 計測日 | dotfiles (open / in_progress / closed) | agentic-coding-tools (同) | active 合計 |
| --- | --- | --- | --- |
| 2026-08-25 | 内訳を取っていない | 内訳を取っていない | 46 |
| 2026-08-27 | 29 / 2 / 21 | 22 / 1 / 8 | 54 |
| 2026-08-28 | 37 / 2 / 24 | 29 / 2 / 11 | 70 |
| 2026-09-02 | 42 / 2 / 29 | 33 / 2 / 14 | 79 |

08-28 の増分の内訳は、この Issue の作業で起票した 5 件 (ISSUE-59 から ISSUE-63) と割り込み中に
起票したもので、closed 側の増分は ISSUE-54 / ISSUE-55 / ISSUE-56 である。

09-02 の数え方は、`issue.md` を持つディレクトリを `closed/` 込みで走査し `templates/` を除いた
もの。`status` frontmatter とディレクトリ位置のクロス集計も取り、`closed/` 配下 29 件と
`status: closed` 29 件が完全一致すること、`status` 行を持たない `issue.md` が 0 件であることを
確かめた。Issue 18 が扱う不整合は 2026-09-02 時点では発生していない。

**件数が増え続けていることそのものは、この Issue の目的が達成されていないことを示さない。**
整理の目的は俯瞰を持つことで、件数を減らすことではない。

数え直しの失敗は 2 回ある。08-27 は一覧を目で数えて 29 と報告して実際は 28、09-02 は同じく目で
数えて open 41 と報告して機械では 42 だった。**どちらもこの節を読みながら踏んでいる。**

09-02 の作業中にも上流が 1 件増えた。着手時に測った上流 open 32 が、同じセッションの終わりには
33 になっている。並行して別のセッションが起票しているためで、**同じ日付でも値は動く**。上の表の
09-02 行は後から測り直した値である。

### M1 が M2 に先行する理由

M2 の baseline は違反をファイルパスと語の組で記録する。`closed/` への移動がパスを変えるので、
移動の扱いが決まる前に baseline を作ると作り直しになる。

この制約が掛かるのは baseline を作る作業だけである。M2 のうち ISSUE-53 が該当し、ISSUE-64 と
ISSUE-72 は該当しない (2026-09-02 に射程を確かめた)。

### M1 の裁定内容

Issue 43 が挙げる 3 案のいずれでもない形を採る。

- `closed/` への移動は維持する。一覧で open と closed が区別できなくなるのを避けるため
- Issue 間の参照はリンクを張らず識別子だけにする
- 識別子の実在を機械が検査し、参照の腐りを検出する

失うのは Issue 本文からのクリック導線。エージェント側の導線は skill の Phase C.3 が持つ
識別子からの検索が代替する。

規約の canonical は skill 側なので、上流へ起票して裁定する。上流 ISSUE-24 が同じ対象
(Issue 間の相対リンクの書式) を扱っており、そちらはリンクを張るかどうかを未決のまま
持っているので、同じテーブルで決めること。

### M2 は前提の組み直しから始まる

「配布先で検査が取り付けられているかを見る層が無い」という問題に触れている Issue は上流に
9 件あるが、担当している Issue は 1 件も無かった。担当は ISSUE-53 として起票済みで、
次の前提もそちらへ引き継いである。

当初は消費側 (config-guard) に ratchet を載せる形を想定していたが、これは dotfiles だけの
機構で、pre-commit 自体を持たないリポジトリには載せる土台が無い。「検査を配布先の
git hook / CI から呼ぶ」という土俵そのものを疑うところから設計し直す。

## タスク

- [x] 両リポの open Issue をクラスタへ整理し、順序制約を本文から抽出する
- [x] Issue 43 の 3 案を実測で評価し、採る形を決める
- [x] 所属と先行関係を各 Issue へ書き戻すかを決める
      所属はこの Issue を canonical とし、書き戻さないことにした。両側に書くと二重管理になり、
      Issue が動くたびに 2 箇所を直すことになる。既に書かれていた 5 件も外した。先行関係だけは
      着手時に読むのが後続側の本文なので、欠けていた 4 組を書き足した
- [ ] 起票の手順へ「マイルストーンへの所属を決める」を入れるか決める。08-28 と 09-02 の 2 回、
      割り込みで起票した Issue がまとめて俯瞰から漏れた。手順の canonical は上流の
      `dev-workflow:in-repo-issue` skill なので、入れるなら上流への委譲になる
- [ ] stale な参照を訂正する。dotfiles 側の記法不一致 10 件は 2026-08-28 に揃えた。
      2026-09-02 に参照の実在を機械で走査したところ、存在しない番号を指す参照は両リポとも
      0 件だった (壊れた参照を置いた対照つきで確かめた)。closed を指す参照を持つ Issue は
      40 件あるが、解決済みの経緯を指す正当な参照が多数を占めるので機械では stale と
      区別できない。上流側の記法不一致はまだ数えていない
- [x] M1 を上流へ起票し、ISSUE-24 と同じテーブルで裁定する。裁定の結果「新規 Issue は
      立てず追記」と決まり、上流 ISSUE-24 の裁定節へ追記して closed になっている
- [x] M2 の担当 Issue を起票する (ISSUE-53。前提の組み直しはそちらが持つ)

## 関連

参照は識別子だけで書く。他リポジトリを指すときはリポジトリ名を前置し、前置を行をまたいで
共有しない。1 行ずつ独立に解決できる形にしておくと、行を動かしても指す先が変わらない。
この Issue 自身が M1 の裁定内容の先行実装にあたる。

所属マイルストーンはここには書かない。上のマイルストーン表が canonical で、両方に書くと
Issue が動くたびに 2 箇所を直すことになる。ここに残すのは表から読み取れない関係だけである。

- Issue 43: 3 案の評価結果をこの Issue が持つので、着手時に突き合わせること
- Issue 17 と Issue 18: M1 の裁定でディレクトリ構造が変わらないことが決まったので、
  両者の前提は保たれる
- ISSUE-52: 稼働中の別プロジェクトから委譲され、dotfiles で再現して境界を詰めた。上流へ委譲する
- ISSUE-50: ruleset 自体は既に active で PR 必須 / force push 禁止 / 削除禁止を課しているが、
  必須ステータスチェックを持たないので checks 0 件の PR も CLEAN になる。そこが残件
- ISSUE-63: 再現していない観測の記録で、再現しないまま一定期間が過ぎたら記録として閉じてよい
- ISSUE-51: ISSUE-49 からの派生
- Issue 31: stale な参照を持つ
- agentic-coding-tools の ISSUE-22: 記法混在の追跡。rename しない判断と、取り付けコストの
  見積もりの残タスクを持つ
- agentic-coding-tools の ISSUE-24: Issue 間の相対リンクの書式。M1 の裁定を持ち込んだ先で、
  持ち込みの結果 closed になっている
- agentic-coding-tools の ISSUE-23: 露出スイープの保留分。ISSUE-15 の後続にあたる
- agentic-coding-tools の ISSUE-16: 配布先での偽陽性。「配る前に完了させる」というゲート
  宣言を持つが、配布は既に完了しているためゲートとして機能していない
- agentic-coding-tools の ISSUE-12 と ISSUE-13: 検査自身の検査と、両取り付けの同時撤去の検出
