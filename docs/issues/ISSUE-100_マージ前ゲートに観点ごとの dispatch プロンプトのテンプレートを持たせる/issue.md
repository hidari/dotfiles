---
status: open
---

# agent: マージ前ゲートに観点ごとの dispatch プロンプトのテンプレートを持たせる

## 背景

非公開の利用側リポジトリから委譲された (2026-09-17)。ISSUE-99 と同じ委譲元で、マージ前の横断
レビューを 3 巡回した実測から出てきた。

ユーザーの裁定は次のとおり。

- テンプレートは `dev-workflow:pre-merge-quality-gate` の中に置く
- 観点は束ねず、いまの観点別の分け方を保つ。観点の数は起動条件 (E2E の影響確認と PUBLIC 漏洩
  スイープ) で変わるので、skill に数を literal で書かない

### 今の gate (v0.7.0 と v0.8.0 で SKILL.md は同一)

- 観点ごとの prompt は Phase 1 の番号付きリストへ直書きされていて、共通のブロックは無い。書き方も
  観点ごとに違う
  - simplify と E2E の影響確認は、skill へ渡す args だけを書く
  - code-reviewer は 1 行の要素の列挙
  - Boy Scout と PUBLIC 漏洩スイープは逐語の長文
- 事実上の共通要素は、観点ごとに別の文言で重複している。確信度のしきい値、0 件の明示、
  `<file>:<line> → ...` の出力形の 3 つ
- 集約出力は観点ごとの `completed / skipped (reason)` の 1 行で、担当外の発見や 0 件の根拠を
  受ける欄が無い
- 出力フォーマット節は、Boy Scout の漏れを「プロンプト改善対象として記録する」と書くが、記録先が無い

### 実測 (利用側リポジトリ)

- コントローラは dispatch prompt を毎回 3〜4k トークン手書きしており、自身のコンテキストを圧迫していた
- 2 巡目で最も重い欠陥を出したのは Boy Scout の観点だった。与えた 4 つのカテゴリのどれにも
  当たらないものを、担当外として別枠で報告してきた。担当を狭めすぎると、範囲外の最も重い欠陥を
  取りこぼす
- 3 巡目は全ブリーフに担当外の枠を置いた。枠から 3 件が出て、2 件が反証を生き残った。最も多くの
  観点 (5 つ) が独立に指した指摘のうち 2 件は、枠から出ていた
- 観点どうしは正反対を返す (simplification と altitude、reuse と efficiency)。束ねる案は衝突しやすい
  組を分ける限り成立しうるが、束ねないと決めた

### テンプレートの共通要素

利用側で再利用できると分かった要素。

1. コントローラの理解を鵜呑みにせず実測で確かめ、食い違ったら実測を正とする
2. 作業ツリーを変更せず、実験は隔離したコピーで行う
3. さらに subagent を起動しない
4. 0 件なら、何を何件読んだ結果かを添えて明示する
5. 環境の制約 (認証、隔離など) を伝えるときは、回避の禁止まで併記する
6. 担当外の発見を返す枠
7. リポジトリ固有の誤検出源 (日付の表記揺れ、注入待ちのプレースホルダ、正当な WHY コメントなど) を差し込む口

今の gate の prompt 群に、1〜3、5、7 は無い。6 は Boy Scout だけが一部を持つ (隣接ファイルの
候補と、PR の範囲を大きく超えるリファクタを、別 Issue の提案として返す)。4 は、件数まで求めるのが
PUBLIC 漏洩スイープだけで、Boy Scout は 0 件の明示だけ、code-reviewer には規定そのものが無い。

## 決めること

- **テンプレートの形と置き場。**SKILL.md の本文か、同梱のファイルか。共通部と観点固有部をどう
  分けるか。観点を 1 つ足す単位がテンプレート 1 枚になる (上流 ISSUE-61 の未決に関わる)。
  gate が関連に挙げる superpowers の requesting-code-review は、同梱ファイルとプレースホルダの
  一覧の形で dispatch テンプレートを持ち、2 と 3 に当たる節も含む。出発点にできる
- **CLAUDE.md との関係。**このリポジトリのグローバル CLAUDE.md は、要素ごとに向きが違う。
  - 1、3、4 は subagent 自身への規範で、subagent にも注入される。テンプレートへ写すと同じ規範が
    2 箇所にできるので、CLAUDE.md を持たない配布先のために写すのか、参照にとどめるのかを決める。
    写すなら、写しの drift を見る手段も決める
  - 2 と 5 は、委譲する側に brief へ書くことを求める規範である。テンプレートはその義務を満たす
    手段にあたり、写しにはならない
  - 6 と 7 は CLAUDE.md に無く、テンプレートに書かないと届かない
- **受け側。**担当外の発見と 0 件の根拠を受ける欄が要る。欄の形は上流が持つ。skip の根拠を
  受ける形を決めるタスクは上流 ISSUE-59 にあり、出力フォーマット節の欠陥を扱う上流 ISSUE-33 の
  追記と衝突しないことを条件にしている (どちらも未了)。この Issue は要件と実測を材料として
  両方へ渡す。出力フォーマット節の「プロンプト改善対象として記録する」の記録先も同じ節の話なので、
  同じく渡す
- **skill 経由の観点への届け方。**simplify と E2E の影響確認は、gate 側で prompt を持たない。共通要素を args に載せるか、届かないと明記するか
- 起動数の写しの置き場を、上流 ISSUE-47 と同時に決める
- **反証の工程を含めるか。**利用側で試行中で、結果が届いてから決める (それまでは含めない)。基準にした 3 巡目では、
  反証 (指摘 1 件に 1 体) がワークフロー全体の費用の 73% を占めた。反証は 30 件中 10 件を落とし、
  そのうち 4 件は別の観点が出した同じ指摘が生き残っていたので、コントローラが反証を退けて直した

## タスク

- [ ] テンプレートの形と置き場を決める。skill 経由の観点への届け方、環境に依存する要素、
      上流 ISSUE-47 との同時決定を含める
- [ ] CLAUDE.md の規範との関係を決める
- [ ] 受け欄の要件と、「プロンプト改善対象」の記録先の問いを、上流 ISSUE-33 と上流 ISSUE-59 へ
      材料として渡す
- [ ] 比べるなら、導入前に dispatch prompt の手書きの量と担当外の枠から出た指摘の件数を、数え方を
      決めて取り直すよう委譲元へ頼む (上の実測の値は数え方の記録が無い)
- [ ] agentic-coding-tools へ実装を委譲する。上流 ISSUE-47 の関連へこの Issue を足すことも頼む
- [ ] 利用側の反証の試行結果を受け取り、反証の工程を足すかを決める。結果は委譲元からの新しい委譲と
      して届く。届くまでは反証の工程を含めずに進める
- [ ] 導入後に、同じ数え方で手書きの量と担当外の枠から出た指摘の件数を記録する

## 関連

- ISSUE-99: 同じ委譲元からの依頼 (plan のコード片)
- agentic-coding-tools の ISSUE-47: gate の起動数の写し。テンプレート化で置き場が動く
- agentic-coding-tools の ISSUE-33: gate の出力フォーマット節が空虚な合格を許す件。受け欄の形に関わる
- agentic-coding-tools の ISSUE-59: E2E の skip の根拠が集約出力へ流れない件。skip の根拠を受ける形を決めるタスクを持ち、受け欄の形に関わる
- agentic-coding-tools の ISSUE-61: 配布物が配布先で成立しているかを見る層が無い件。観点を足す場所を
  未決で持ち、6 の環境依存は B 面の一例にあたる
- agentic-coding-tools の ISSUE-57: 別の skill のたたき台が subagent ごとの dispatch prompt の雛形を
  計画している。形を揃えられる
- agentic-coding-tools の ISSUE-55: gate の観点より広い 8 観点で並列レビューを回した実例
