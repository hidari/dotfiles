# archive

ここにあるのは、Issue との対応が 1 対 1 で確定しなかった過去の設計 (spec) と計画 (plan) である。

文書内のパス参照は移行前のディレクトリ構成 (`docs/superpowers/plans/` と `docs/superpowers/specs/`) を指したままにしてある。これは移行時に書き換えず、当時の記録としてそのまま残した結果である。

現行の成果物の置き場規約は `dev-workflow:issue-scoped-artifacts` skill を canonical とする。新規の設計・計画はそちらの規約に従うこと。
