# mise が管理する global ツールの pin。exact 指定で全マシンを完全再現する。
#
# 射程はマシン共通で要る版だけ。プロジェクト固有の版は各リポジトリの mise 設定が持ち、そちらを
# CI やテストが版の canonical として読んでいることがあるので、ここの方針をそのまま適用しない。
# 版を pin しないマシン共通のツールの宣言先は home/.Brewfile。
[tools]
node = "24.18.0"
